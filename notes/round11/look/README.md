# Round 11: three whole winter paintings (D, E, F), unlabeled
Each whole at 1000 wide (the 3200 render scaled down, not a separate render) and the 3200 render. Three different models; the key comes after Alice has looked.
