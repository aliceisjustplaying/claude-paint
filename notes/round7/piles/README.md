# Piles: the evening_lake sky and lake, formula vs piles (Round 7)

Same painting, same composition, same seeds; one version paints the sky,
the cloud bank, its lights and the open water from the painter's color
formulas, the other from a few hand-mixed piles (`piles{}`). Which is
which is in `key.md` (look first, then decode).

- `piles_alice.png` (lossless): the whole at 1000, A beside B, over three
  1:1 crops at 3200 (glow and cloud bank; upper sky and moon; open water).
- `A_1000.png`, `B_1000.png`, `A_3200.png`, `B_3200.png`: the plain
  paintings (lossless).
- `evening_lake_piles.lua`: the piles version's log (the painter's log from
  branch `r7-paint-wet` with five lines changed; `evening_lake_piles.diff`).
  Render it with an easel that has both the wet engine and `piles`
  (`r7-paint-wet` plus this branch's commits).

Write-up: `notes/piles.md`.

(Copied from branch r7-piles for viewing; the piles helper itself is not merged: see notes/principles.md and notes/round6/alice_review.md.)
