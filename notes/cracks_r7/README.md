# Round 7 craquelure: evidence (lossless)

*Evening at a Mountain Lake* (log on `r7-paint-wet`, `cracks{}` added before
`relief()` in the last chunk), cracked with the Round 6 code (before) and
the Round 7 code (after). Write-up: `notes/cracks.md`, "Round 7".

- `evening_lake_cracks_before_after_3200.png`: 1:1 crops of the 3200 render,
  before | after: sky and moon, the lake, the dark fir wood against the glow.
- `before_sky.png`, `after_sky.png`, `before_water.png`, `after_water.png`,
  `before_firs.png`, `after_firs.png`: the same crops, plain (800 × 560 at
  3200: sky at pixel (1560, 300), water at (1700, 1500), firs at (300, 820)).
- `evening_lake_cracks_before_after_1000.png`: the whole at 1000, each
  cracked at 1000.
- `evening_lake_aged_r7_1000.png`, `evening_lake_aged_r7_3200.png`: the
  plain aged painting with the new cracks.
- `raking_light_water_3200.png`: the lake crop lit with `relief(0.3)`
  instead of the style's 0.06, Round 6 recipe (`Cracks::even`) | Round 7:
  the cupped islands.
