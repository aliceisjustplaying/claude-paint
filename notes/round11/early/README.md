# Stop early: does the digital look come from the last passes?
Three finished programs (Round 11 E = Astra, D = Fable, Round 10 C = the
pond), re-rendered at 3200, stopped at earlier stages (`--resume S --stop S`).
- `*_stages.png`: 1 finished; 2 all stages without the finish (varnish,
  cracks, relief light); 3-5 stopped one, two, three stages earlier.
- `*_crops_1to1.png`: the lower foreground at 1:1: finished, all stages
  without the finish, three stages early.
Caveat: a stopped render has no finish at all, so compare 2 with 3-5 for
the late passes; 1 against 2 shows the finish alone.
