# Round 13: three hand-structured trees, unlabeled (S, T, U)
`sheet_STU.png` side by side; `compare_r12_r13.png`: Round 12 (grown by the engine) above Round 13 (no growth model). Each whole (`S.png`, scaled) and the 2400 render (`S_2400.png`); look at the 2400 files at 100%. Key after Alice looks.
Note: all three placed the trunk and main limbs by hand, then wrote their own rules (the area rule, a kink at each fork) to fill in the fine branches.
